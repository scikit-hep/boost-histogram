[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "numpy",
    "setuptools_scm[toml]>=4.1.2"
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/boost_histogram/version.py"
